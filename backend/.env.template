# Telegram bot environment variables
# Copy this file to .env and fill in values before running the bot.

# Your bot token from @BotFather
BOT_TOKEN=
TELEGRAM_BOT_TOKEN=

# Public base URL where Telegram can reach your webhook (no trailing slash)
# Example using cloudflare tunnel: https://abcd-1234.trycloudflare.com
WEBHOOK_URL_BASE=

# Secret path token used to validate webhook requests (optional)
WEBHOOK_SECRET=secret

# Backend base URL used by /status and /echo commands
BACKEND_BASE=http://localhost:5000

# Port for the local Express server
PORT=3000

# Environment
NODE_ENV=development

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/telegram_bot

# Encryption - IMPORTANT: Keep this secure and never commit to git!
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_MASTER_PASSWORD=

# API Configuration
API_PORT=3000

# Sanctum Gateway Configuration
# Get your API key from: https://gateway.sanctum.so/
SANCTUM_API_KEY=
SANCTUM_CLUSTER=mainnet  # or 'devnet' for testing
